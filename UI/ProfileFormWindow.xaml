<Window x:Class="ProxySwitcher.UI.ProfileFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Proxy Profile"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        Foreground="#333333"
        FontSize="12">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="0,10,0,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Height" Value="32"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <StackPanel VerticalAlignment="Top">
            <!-- Title -->
            <TextBlock Text="Profile Details" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- Profile Name -->
            <Label Content="Profile Name *"/>
            <TextBox x:Name="NameTextBox" />

            <!-- Proxy Host -->
            <Label Content="Proxy Host (IP or Domain) *"/>
            <TextBox x:Name="HostTextBox" />

            <!-- Proxy Port -->
            <Label Content="Proxy Port (1-65535) *"/>
            <TextBox x:Name="PortTextBox" />

            <!-- Options -->
            <StackPanel Margin="0,15,0,0">
                <CheckBox x:Name="EnableSystemProxyCheckBox" 
                          Content="Apply to System Proxy Settings" 
                          IsChecked="True"
                          Margin="0,5,0,5"
                          FontWeight="SemiBold"/>
                <CheckBox x:Name="EnableEnvironmentVariablesCheckBox" 
                          Content="Set Environment Variables (HTTP_PROXY, HTTPS_PROXY, ALL_PROXY)" 
                          Margin="0,5,0,15"
                          FontWeight="SemiBold"/>
            </StackPanel>

            <!-- Error Message -->
            <TextBlock x:Name="ErrorMessage" 
                       Foreground="Red" 
                       TextWrapping="Wrap" 
                       Margin="0,10,0,10" 
                       Visibility="Collapsed"/>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button x:Name="SaveButton" Content="Save" Click="SaveButton_Click" Background="#4CAF50" Foreground="White"/>
                <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click" Background="#757575" Foreground="White"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
